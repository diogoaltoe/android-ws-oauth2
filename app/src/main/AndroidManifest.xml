<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ws.droid">

    <!-- Request Internet connection -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Request to check Internet connection state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="ws.droid.controller.SessionController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            Main Activity
        -->
        <activity android:name="ws.droid.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            User New Activity
        -->
        <activity
            android:name="ws.droid.activity.UserNewActivity"
            android:label="@string/title_activity_user_new"
            android:parentActivityName="ws.droid.activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.MainActivity" />
        </activity>

        <!--
            User Login Activity
        -->
        <activity
            android:name="ws.droid.activity.UserLoginActivity"
            android:label="@string/title_activity_user_login"
            android:parentActivityName="ws.droid.activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.MainActivity" />
        </activity>

        <!--
            User Home Activity
        -->
        <activity
            android:name="ws.droid.activity.UserHomeActivity"
            android:label="@string/title_activity_user_home">
        </activity>

        <!--
            User Main Activity
        -->
        <activity
            android:name="ws.droid.activity.UserMainActivity"
            android:label="@string/title_activity_user"
            android:parentActivityName="ws.droid.activity.UserHomeActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.UserHomeActivity" />
        </activity>

        <!--
            User List Activity
        -->
        <activity
            android:name="ws.droid.activity.UserListActivity"
            android:label="@string/title_activity_user_list"
            android:parentActivityName="ws.droid.activity.UserMainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.UserMainActivity" />
        </activity>

        <!--
            User Edit Activity
        -->
        <activity android:name="ws.droid.activity.UserEditActivity"
            android:label="@string/title_activity_user_edit"
            android:parentActivityName="ws.droid.activity.UserListActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.UserListActivity" />
        </activity>


        <!--
            Product Main Activity
        -->
        <activity
            android:name="ws.droid.activity.ProductMainActivity"
            android:label="@string/title_activity_product"
            android:parentActivityName="ws.droid.activity.UserHomeActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.UserHomeActivity" />
        </activity>

        <!--
            Product New Activity
        -->
        <activity
            android:name="ws.droid.activity.ProductNewActivity"
            android:label="@string/title_activity_product_new"
            android:parentActivityName="ws.droid.activity.ProductMainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.ProductMainActivity" />
        </activity>

        <!--
            Product List Activity
        -->
        <activity
            android:name="ws.droid.activity.ProductListActivity"
            android:label="@string/title_activity_product_list"
            android:parentActivityName="ws.droid.activity.ProductMainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.ProductMainActivity" />
        </activity>

        <!--
            Product Edit Activity
        -->
        <activity android:name="ws.droid.activity.ProductEditActivity"
            android:label="@string/title_activity_product_edit"
            android:parentActivityName="ws.droid.activity.ProductListActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.ProductListActivity" />
        </activity>


        <!--
            Person Main Activity
        -->
        <activity
            android:name="ws.droid.activity.PersonMainActivity"
            android:label="@string/title_activity_person"
            android:parentActivityName="ws.droid.activity.UserHomeActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.UserHomeActivity" />
        </activity>

        <!--
            Person New Activity
        -->
        <activity
            android:name="ws.droid.activity.PersonNewActivity"
            android:label="@string/title_activity_person_new"
            android:parentActivityName="ws.droid.activity.PersonMainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.PersonMainActivity" />
        </activity>

        <!--
            Person List Activity
        -->
        <activity
            android:name="ws.droid.activity.PersonListActivity"
            android:label="@string/title_activity_person_list"
            android:parentActivityName="ws.droid.activity.PersonMainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.PersonMainActivity" />
        </activity>

        <!--
            Person Edit Activity
        -->
        <activity android:name="ws.droid.activity.PersonEditActivity"
            android:label="@string/title_activity_person_edit"
            android:parentActivityName="ws.droid.activity.PersonListActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ws.droid.activity.PersonListActivity" />
        </activity>
    </application>

</manifest>